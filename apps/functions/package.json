{
  "name": "@dvcs/functions",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "dev": "pnpm build --watch",
    "build": "tsc",
    "lint": "eslint src",
    "typecheck": "tsc --noEmit",
    "serve": "pnpm build && firebase emulators:start --only functions"
  },
  "dependencies": {
    "@dvcs/validation": "workspace:*",
    "firebase-admin": "^13.6.0",
    "firebase-functions": "^7.0.3",
    "zod": "^4.3.5"
  },
  "devDependencies": {
    "@dvcs/config": "workspace:*",
    "typescript": "^5.9.3"
  }
}
