# Server-only values. Use Firebase parameterized configuration or Secret Manager in production.
APP_ENV=development
DEFAULT_GRACE_PERIOD_DAYS=7
# Generate with: openssl rand -base64 32
# Store with Firebase Secret Manager; never commit the actual value.
COMMUNICATION_CREDENTIAL_MASTER_KEY=
